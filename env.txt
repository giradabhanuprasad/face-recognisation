# Flask Configuration
SECRET_KEY=your-secret-key-here  # Keep this unique and secure
FLASK_ENV=development           # Set to 'production' for deployment
FLASK_DEBUG=1    
FLASK_APP=app.py               # Set to 0 for production

# Database Configuration
DATABASE_URL=postgresql://postgres:root@localhost:5432/attendance_system  # Update 'root' with your actual password

# Face Recognition Configuration
FACE_RECOGNITION_THRESHOLD=0.6
FACE_DETECTION_CONFIDENCE=0.7

# Attendance Configuration
WORKING_HOURS_START=09:00        # Format HH:MM (24-hour)
WORKING_HOURS_END=17:00          # Format HH:MM (24-hour)
LATE_THRESHOLD_MINUTES=15        # Adjusted to 30 minutes for more realistic late detection

# Security Configuration
SESSION_LIFETIME_HOURS=8         # Reasonable duration
MAX_LOGIN_ATTEMPTS=3             # Increased to 5 for better usability
LOGIN_TIMEOUT_MINUTES=15         # Kept as is

# Email Configuration (Gmail)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=jagadeeshlenka66@gmail.com    # Replace with your Gmail address
MAIL_PASSWORD=eblj tvps ugsi dnev    # Replace with the App Password you generated
MAIL_DEFAULT_SENDER=jagadeeshlenka66@gmail.com    # Replace with your Gmail address